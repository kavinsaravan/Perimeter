var length = window.prompt("Enter length of a rectangle:");
var width = window.prompt("Enter width of a rectangle:");
var sum = parseInt(length) + parseInt(width)
alert("The perimeter is" + 2 * (sum));